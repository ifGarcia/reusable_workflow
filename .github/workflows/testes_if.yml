name: if teste

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  job_ubuntu:
    runs-on: ubuntu-latest
    environment: DEV
    
    steps:

    - if: ${{ vars.ENVIRONMENT_NAME }}
      run: |
        echo ${{ vars.ENVIRONMENT_NAME }}
      
    - if: ${{ vars.ENVIRONMENT_NAME == 0 }}
      run: |
        echo "tudo certo 2"
